# Копируйте в .env и подставьте свои значения.
# Используется для: docker compose -f docker-compose.prod.yml --env-file .env up -d

# ---- Порты на хосте (если 80/5432 уже заняты) ----
HTTP_PORT=8080
POSTGRES_PORT=5432

# ---- Безопасность (обязательно сменить на сервере) ----
JWT_SECRET=your-super-secret-key-change-in-production
POSTGRES_USER=chat
POSTGRES_PASSWORD=chat_secret
POSTGRES_DB=chat_db

# ---- Фронт (оставьте пустым при одном домене/порте) ----
# Если приложение открывается по https://example.com — оставьте пустым.
# Если API на другом домене — укажите полный URL, например: https://api.example.com
VITE_API_URL=
